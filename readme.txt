hello git

Useful Commands:

git status
git log
git add .
git commit -m”Hello”
git push 


# gitignore -  A gitignore file specifies intentionally untracked files that Git should ignore.


# all_git_codes - All the git console codes that are done are pasted here .  